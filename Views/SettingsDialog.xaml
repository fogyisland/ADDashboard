<Window x:Class="PackageDesigner.Views.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Width="520" Height="240" WindowStartupLocation="CenterOwner">
  <StackPanel Margin="20">
    <TextBlock Text="Settings" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,4"/>
    <TextBlock Text="Connection details for the Center service this designer publishes to."
               Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="0,0,0,16"/>
    <Grid Margin="0,0,0,8">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="120"/><ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition/><RowDefinition/>
      </Grid.RowDefinitions>
      <TextBlock Grid.Row="0" Grid.Column="0" Text="Center URL" Style="{StaticResource FieldLabel}"/>
      <TextBox   Grid.Row="0" Grid.Column="1" x:Name="UrlBox" Text="{Binding CenterUrl, UpdateSourceTrigger=PropertyChanged}"/>
      <TextBlock Grid.Row="1" Grid.Column="0" Text="API token"  Style="{StaticResource FieldLabel}"/>
      <PasswordBox Grid.Row="1" Grid.Column="1" x:Name="TokenBox"/>
    </Grid>
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
      <Button Content="Cancel" Width="90" Margin="0,0,8,0" Click="Cancel_Click" IsCancel="True"/>
      <Button Content="Save" Width="90" Click="Save_Click" IsDefault="True" Style="{StaticResource PrimaryButton}"/>
    </StackPanel>
  </StackPanel>
</Window>
