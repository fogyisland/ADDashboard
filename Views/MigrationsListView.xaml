<UserControl x:Class="PackageDesigner.Views.MigrationsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <DockPanel>
    <ListBox x:Name="List" ItemsSource="{Binding Items}" BorderThickness="0,0,0,1"/>
    <DockPanel Margin="0,8,0,0">
      <Button DockPanel.Dock="Right" Content="+" Width="32" Margin="6,0,0,0" Click="Add_Click" ToolTip="Add migration"/>
      <Button DockPanel.Dock="Right" Content="−" Width="32" Margin="6,0,0,0" Click="Remove_Click" ToolTip="Remove selected"/>
      <TextBox x:Name="PathBox"/>
    </DockPanel>
  </DockPanel>
</UserControl>
