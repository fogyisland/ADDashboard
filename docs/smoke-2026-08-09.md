# WPF Package Designer — Manual Smoke Test (smoke-2026-08-09)

> **Status: GUI steps PENDING — not yet executed.**
>
> The publish step below was executed (real, observed output). The eleven
> GUI interaction steps have **not** been run. This file is a procedure
> awaiting execution by a human operator on a Windows 11 VM. Each step
> ends with `Result: PENDING — not yet executed.`; the controller will
> fill in the final results after the operator finishes.
>
> Do not interpret any line in the GUI section as an observed result.

## 1. publish (executed by implementer)

Command:

```
dotnet publish PackageDesigner.csproj -c Release -r win-x64 --self-contained
```

Result (real, observed):

- Exit code: `0`
- Binary produced: `bin/PackageDesigner/Release/net8.0-windows/win-x64/publish/PackageDesigner.exe`
- `.exe` size: `151,552 bytes` (~148 KiB — the managed WPF bootstrapper)
- Full self-contained publish directory: `bin/PackageDesigner/Release/net8.0-windows/win-x64/publish/`
- Files in publish directory: `262`
- Total publish size: `163 MB` (self-contained: includes .NET 8 runtime + WPF assemblies + Avalonia-style Managed+Native dependencies)
- No errors, no warnings.

## 2. GUI smoke test steps (PENDING — awaiting human operator)

Run the following on a Windows 11 VM. Operate `PackageDesigner.exe`
from `bin/PackageDesigner/Release/net8.0-windows/win-x64/publish/`.

1. `dotnet publish PackageDesigner.csproj -c Release -r win-x64 --self-contained` produces `bin/Release/net8.0-windows/win-x64/publish/PackageDesigner.exe`.
   Result: PENDING — not yet executed.

2. Launch the .exe.
   Result: PENDING — not yet executed.

3. File → New → choose `ad-os-baseline-lite` template → name "smoke-test" → OK.
   Result: PENDING — not yet executed.

4. Verify the Manifest tab shows `agent.type = Non-Ad` in the dropdown.
   Result: PENDING — not yet executed.

5. Edit `migrations/001_initial.sql` to insert `DROP TABLE foo` after the CREATE → status strip shows X (fail).
   Result: PENDING — not yet executed.

6. Edit `collect.ps1` with `Get-Process` → no status strip (PS1 has no sandbox).
   Result: PENDING — not yet executed.

7. File → Save → choose `<workspace>/smoke-test.pkgproj` → file persists.
   Result: PENDING — not yet executed.

8. Re-launch the .exe → no recovery dialog (no `.auto-save.log` written yet).
   Result: PENDING — not yet executed.

9. Quit while editing → relaunch → recovery dialog appears (because the auto-save log was written on the first quit).
   Result: PENDING — not yet executed.

10. Settings → set Center URL + token → Save.
    Result: PENDING — not yet executed.

11. Publish → "Published OK" or a meaningful error code/message.
    Result: PENDING — not yet executed.

## Notes for the operator

- The .exe is in `bin/PackageDesigner/Release/net8.0-windows/win-x64/publish/PackageDesigner.exe`.
- Target framework: `net8.0-windows`; runtime: `win-x64`; deployment: self-contained (no .NET install required on the VM).
- The Windows 11 VM needs write access to the chosen `<workspace>` directory for Save / auto-save / recovery log files.
- The Center URL + token in step 10 must point at a running AD Dashboard center that has the v2 package install endpoint (`POST /api/admin/packages/install`) reachable from the VM. The PublishService posts a JSON+base64 envelope; see `Services/PublishService.cs`.
- If any step fails, record the actual observed behavior (error message, screenshot path, exit code) verbatim — do not paraphrase.
